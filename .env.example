# Database URL — defaults to embedded SQLite at data/app.db
# To use PostgreSQL, set: DATABASE_URL=jdbc:postgresql://host:5432/dbname?user=...&password=...
# DATABASE_URL=

# Server port — defaults to 3000
# PORT=3000
